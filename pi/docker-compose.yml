version: '3.8'

services:
  webcam-stream:
    build: .
    container_name: webcam-stream
    restart: unless-stopped
    devices:
      - /dev/video0:/dev/video0
    ports:
      - "8000:8000"
    networks:
      - webcam-net

networks:
  webcam-net:
    driver: bridge
